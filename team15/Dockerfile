# Team 15 uses the shared root Django service (`core`) from /Dockerfile.
# Keep this as a placeholder for optional team-owned side services only.
FROM alpine:3.20
CMD ["sh", "-c", "echo 'Team15 uses shared core service; replace this Dockerfile only for extra side services.'; sleep infinity"]
